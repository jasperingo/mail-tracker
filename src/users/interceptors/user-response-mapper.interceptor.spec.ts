import { UserResponseMapperInterceptor } from './user-response-mapper.interceptor';

describe('UserResponseMapperInterceptor', () => {
  it('should be defined', () => {
    expect(new UserResponseMapperInterceptor()).toBeDefined();
  });
});
