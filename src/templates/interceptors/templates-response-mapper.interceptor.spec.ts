import { TemplatesResponseMapperInterceptor } from './templates-response-mapper.interceptor';

describe('TemplatesResponseMapperInterceptor', () => {
  it('should be defined', () => {
    expect(new TemplatesResponseMapperInterceptor()).toBeDefined();
  });
});
