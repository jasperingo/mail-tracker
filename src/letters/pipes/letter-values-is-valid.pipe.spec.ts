import { LetterValuesIsValidPipe } from './letter-values-is-valid.pipe';

describe('LetterValuesIsValidPipe', () => {
  it('should be defined', () => {
    expect(new LetterValuesIsValidPipe()).toBeDefined();
  });
});
