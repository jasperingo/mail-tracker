import { UserPasswordIsValidPipe } from './user-password-is-valid.pipe';

describe('UserPasswordIsValidPipe', () => {
  it('should be defined', () => {
    expect(new UserPasswordIsValidPipe()).toBeDefined();
  });
});
