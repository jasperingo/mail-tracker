import { TemplatesPermissionFactory } from './templates-permission.factory';

describe('TemplatesPermissionFactory', () => {
  it('should be defined', () => {
    expect(new TemplatesPermissionFactory()).toBeDefined();
  });
});
