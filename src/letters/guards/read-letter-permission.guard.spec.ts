import { ReadLetterPermissionGuard } from './read-letter-permission.guard';

describe('ReadLetterPermissionGuard', () => {
  it('should be defined', () => {
    expect(new ReadLetterPermissionGuard()).toBeDefined();
  });
});
