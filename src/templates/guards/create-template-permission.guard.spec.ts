import { CreateTemplatePermissionGuard } from './create-template-permission.guard';

describe('CreateTemplatePermissionGuard', () => {
  it('should be defined', () => {
    expect(new CreateTemplatePermissionGuard()).toBeDefined();
  });
});
