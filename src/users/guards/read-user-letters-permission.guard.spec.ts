import { ReadUserLettersPermissionGuard } from './read-user-letters-permission.guard';

describe('ReadUserLettersPermissionGuard', () => {
  it('should be defined', () => {
    expect(new ReadUserLettersPermissionGuard()).toBeDefined();
  });
});
