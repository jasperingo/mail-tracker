import { TemplateVariablesRepository } from './template-variables.repository';

describe('TemplateVariablesRepository', () => {
  it('should be defined', () => {
    expect(new TemplateVariablesRepository()).toBeDefined();
  });
});
