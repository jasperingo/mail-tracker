import { ReadTemplatesPermissionGuard } from './read-templates-permission.guard';

describe('ReadTemplatesPermissionGuard', () => {
  it('should be defined', () => {
    expect(new ReadTemplatesPermissionGuard()).toBeDefined();
  });
});
