import { CreateUserPermissionGuard } from './create-user-permission.guard';

describe('CreateUserPermissionGuard', () => {
  it('should be defined', () => {
    expect(new CreateUserPermissionGuard()).toBeDefined();
  });
});
