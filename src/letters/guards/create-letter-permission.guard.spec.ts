import { CreateLetterPermissionGuard } from './create-letter-permission.guard';

describe('CreateLetterPermissionGuard', () => {
  it('should be defined', () => {
    expect(new CreateLetterPermissionGuard()).toBeDefined();
  });
});
