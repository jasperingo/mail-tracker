import { RolesPermissionFactory } from './roles-permission.factory';

describe('RolesPermissionFactory', () => {
  it('should be defined', () => {
    expect(new RolesPermissionFactory()).toBeDefined();
  });
});
