import { TemplateVariableDto } from './template-variable.dto';

describe('TemplateVariableDto', () => {
  it('should be defined', () => {
    expect(new TemplateVariableDto()).toBeDefined();
  });
});
