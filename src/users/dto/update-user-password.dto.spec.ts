import { UpdateUserPasswordDto } from './update-user-password.dto';

describe('UpdateUserPasswordDto', () => {
  it('should be defined', () => {
    expect(new UpdateUserPasswordDto()).toBeDefined();
  });
});
