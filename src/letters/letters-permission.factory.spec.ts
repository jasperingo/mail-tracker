import { LettersPermissionFactory } from './letters-permission.factory';

describe('LettersPermissionFactory', () => {
  it('should be defined', () => {
    expect(new LettersPermissionFactory()).toBeDefined();
  });
});
