import { UpdateUserPasswordPermissionGuard } from './update-user-password-permission.guard';

describe('UpdateUserPasswordPermissionGuard', () => {
  it('should be defined', () => {
    expect(new UpdateUserPasswordPermissionGuard()).toBeDefined();
  });
});
