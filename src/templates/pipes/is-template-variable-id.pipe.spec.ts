import { IsTemplateVariableIdPipe } from './is-template-variable-id.pipe';

describe('IsTemplateVariableIdPipe', () => {
  it('should be defined', () => {
    expect(new IsTemplateVariableIdPipe()).toBeDefined();
  });
});
