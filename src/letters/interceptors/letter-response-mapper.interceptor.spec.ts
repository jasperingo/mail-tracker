import { LetterResponseMapperInterceptor } from './letter-response-mapper.interceptor';

describe('LetterResponseMapperInterceptor', () => {
  it('should be defined', () => {
    expect(new LetterResponseMapperInterceptor()).toBeDefined();
  });
});
