import { LetterValuesRepository } from './letter-values.repository';

describe('LetterValuesRepository', () => {
  it('should be defined', () => {
    expect(new LetterValuesRepository()).toBeDefined();
  });
});
