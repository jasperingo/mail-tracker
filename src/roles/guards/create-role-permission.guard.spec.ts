import { CreateRolePermissionGuard } from './create-role-permission.guard';

describe('CreateRolePermissionGuard', () => {
  it('should be defined', () => {
    expect(new CreateRolePermissionGuard()).toBeDefined();
  });
});
