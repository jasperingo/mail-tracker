import { SignLetterPermissionGuard } from './sign-letter-permission.guard';

describe('SignLetterPermissionGuard', () => {
  it('should be defined', () => {
    expect(new SignLetterPermissionGuard()).toBeDefined();
  });
});
