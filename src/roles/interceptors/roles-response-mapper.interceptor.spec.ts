import { RolesResponseMapperInterceptor } from './roles-response-mapper.interceptor';

describe('RolesResponseMapperInterceptor', () => {
  it('should be defined', () => {
    expect(new RolesResponseMapperInterceptor()).toBeDefined();
  });
});
